def merkleTree(hashArray):
#     block = []
#     while len(hashArray) > 1:
#         if len(hashArray) % 2 != 0:
#             for i in range(0, block_len, 2):
#                 hash = block[i] + block[i + 1]
#                 hash = hashlib.sha256(hash.encode()).hexdigest()
#                 block.append(hash)

#                 return block
# # calculateHash(dataBlock)
# # merkleTree(calculateHash)    
# for i in range(block_len):
#     print(merkleTree)    
